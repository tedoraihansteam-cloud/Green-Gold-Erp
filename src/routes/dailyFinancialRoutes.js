const r=require('express').Router(),c=require('../controllers/dailyFinancialController');const {requireAuth}=require('../middleware/auth'),{requirePermission}=require('../middleware/rbac');const {asyncHandler}=require('../middleware/errorHandler');r.use(requireAuth,requirePermission('ACCOUNTS_VIEW'));r.get('/preview',asyncHandler(c.preview));r.get('/',asyncHandler(c.list));r.post('/',requirePermission('ACCOUNTS_CREATE'),asyncHandler(c.prepare));r.post('/:businessId/sign',requirePermission('ACCOUNTS_APPROVE'),asyncHandler(c.sign));module.exports=r;
