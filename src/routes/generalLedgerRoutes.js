const r=require('express').Router(),c=require('../controllers/generalLedgerController');const {requireAuth}=require('../middleware/auth');const {requirePermission}=require('../middleware/rbac');const {asyncHandler}=require('../middleware/errorHandler');r.use(requireAuth,requirePermission('ACCOUNTS_VIEW'));r.get('/chart',asyncHandler(c.chart));r.get('/journals',asyncHandler(c.journals));r.get('/trial-balance',asyncHandler(c.trialBalance));r.get('/profit-loss',asyncHandler(c.profitLoss));r.get('/balance-sheet',asyncHandler(c.balanceSheet));module.exports=r;
