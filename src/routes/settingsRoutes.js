const r=require('express').Router(),c=require('../controllers/settingsController');const {requireAuth}=require('../middleware/auth'),{requirePermission}=require('../middleware/rbac');const {asyncHandler}=require('../middleware/errorHandler');r.use(requireAuth,requirePermission('USER_MANAGEMENT_VIEW'));r.get('/',asyncHandler(c.list));r.put('/:key',requirePermission('USER_MANAGEMENT_APPROVE'),asyncHandler(c.save));module.exports=r;
