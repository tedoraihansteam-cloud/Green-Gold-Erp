import UploadedReferenceHistory from '../../components/UploadedReferenceHistory';
import {useParams} from 'react-router-dom';import {useApi} from '../../lib/useApi';import {downloadApiFile} from '../../lib/apiClient';import DataTable from '../../components/DataTable';import Pill from '../../components/Pill';import BusinessIdentifier from '../../components/BusinessIdentifier';import IdentityCardPanel from '../../components/IdentityCardPanel';
export default function CustomerDetailPage(){const {businessId}=useParams(),{data,loading,error,reload}=useApi(`/customers/${businessId}`),month=new Date().toISOString().slice(0,7);if(loading)return <p>Loading...</p>;if(error)return <div className="error-banner">{error}</div>;const c=data.customer,h=data.history,outstanding=h.receivables.reduce((n,r)=>n+Number(r.outstanding)+Number(r.estimated_penalty),0);return <div><div className="card-header" style={{marginBottom:18}}><div><h1 className="page-title">{c.name}</h1><p className="card-subtitle"><BusinessIdentifier entityType="CUSTOMER" businessId={c.business_id}/> · {c.customer_type||'service customer'}</p></div><div style={{display:'flex',gap:7}}><button className="btn btn-secondary" onClick={()=>downloadApiFile(`/documents/reports/customer-monthly/${businessId}.csv?month=${month}`,`${businessId}-${month}.csv`)}>Monthly Excel</button><button className="btn btn-secondary" onClick={()=>downloadApiFile(`/documents/reports/customer-monthly/${businessId}.pdf?month=${month}`,`${businessId}-${month}.pdf`,true)}>Monthly PDF</button></div></div><div className="stats-grid"><div className="stat-card"><div className="stat-label">Outstanding</div><div className="stat-value">BDT {outstanding.toLocaleString()}</div></div><div className="stat-card"><div className="stat-label">Stored batches</div><div className="stat-value">{h.batches.length}</div></div><div className="stat-card"><div className="stat-label">Deliveries</div><div className="stat-value">{h.deliveries.length}</div></div></div><IdentityCardPanel entityType="CUSTOMER" record={c} name={c.name} onUpdated={reload}/><section className="card"><h2>Due, penalty and payment history</h2><DataTable rows={h.receivables} columns={[{key:'source_id',label:'Source'},{key:'description',label:'Description'},{key:'due_date',label:'Due date',render:r=>String(r.due_date).slice(0,10)},{key:'outstanding',label:'Due',render:r=>`BDT ${Number(r.outstanding).toLocaleString()}`},{key:'estimated_penalty',label:'Penalty',render:r=>`BDT ${Number(r.estimated_penalty).toLocaleString()}`},{key:'status',label:'Status',render:r=><Pill status={r.status}/>}]} /></section><section className="card"><h2>Stock and storage</h2><DataTable rows={h.batches} columns={[{key:'business_id',label:'Batch'},{key:'product_name',label:'Product'},{key:'received_quantity',label:'Received'},{key:'available_quantity',label:'Remaining'},{key:'status',label:'Status',render:r=><Pill status={r.status}/>}]} /></section><section className="card"><h2>Goods receipts and releases</h2><DataTable rows={[...h.goodsReceipts.map(x=>({...x,type:'GRN',quantity:x.received_quantity})),...h.stockReleases.map(x=>({...x,type:'Release'}))]} columns={[{key:'business_id',label:'Document'},{key:'type',label:'Type'},{key:'batch_business_id',label:'Batch'},{key:'quantity',label:'Quantity'},{key:'created_at',label:'Date',render:r=>new Date(r.created_at).toLocaleDateString()}]}/></section><section className="card"><h2>Deliveries and gate passes</h2><DataTable rows={h.deliveries} columns={[{key:'business_id',label:'Delivery'},{key:'delivery_address',label:'Destination'},{key:'vehicle_number',label:'Vehicle'},{key:'gate_pass_business_id',label:'Gate pass'},{key:'status',label:'Status',render:r=><Pill status={r.status}/>}]} /></section><section className="card"><h2>Contracts and requests</h2><DataTable rows={h.requests} columns={[{key:'business_id',label:'Request'},{key:'request_type',label:'Type'},{key:'subject',label:'Subject'},{key:'status',label:'Status',render:r=><Pill status={r.status}/>}]} /></section><UploadedReferenceHistory businessId={businessId}/></div>}
